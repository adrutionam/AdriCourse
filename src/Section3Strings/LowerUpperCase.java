package Section3Strings;

public class LowerUpperCase {
    public static void main(String[] args) {
        String myText = "abcdefg";
        //used to make uppercase or lowercase by force
        System.out.println(myText.toUpperCase());
        String myText2 = "ABCDEFG";
        System.out.println(myText.toLowerCase());
        // toLowerCase(locale locale) is usesd for other languages
    }
}
